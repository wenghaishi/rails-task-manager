<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task[:title], "tasks/#{task.id}" %></li>
    <li><%= task[:completed] %></li>
  <% end %>
</ul>
<%= link_to "Create new task", tasks_new_path %>