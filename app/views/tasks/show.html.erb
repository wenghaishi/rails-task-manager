<h1>Task</h1>
<h2><%= @task.title %></h2>
<h2><%= @task.details %></h2>
<%= link_to "Update task", edit_task_path(@task) %>
<%= link_to "Delete", delete_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
